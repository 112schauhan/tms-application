# Backend Environment Configuration
# Copy this file to .env and update with your values

# Server
NODE_ENV=development
PORT=4000

# Database - Neon PostgreSQL (Required)
# Get connection string from: https://console.neon.tech
# Format: postgresql://user:password@host/dbname?sslmode=require
DATABASE_URL="postgresql://neondb_owner:your-password@ep-your-endpoint.us-east-1.neon.tech/tms_db?sslmode=require"

# JWT Authentication (Required)
# Generate secure random strings - minimum 32 characters each
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-min-32-chars"
REFRESH_TOKEN_SECRET="your-refresh-token-secret-change-this-in-production"

# Frontend URL (for CORS)
# Local dev: http://localhost:5173
# Production: https://your-frontend-domain.vercel.app
FRONTEND_URL="http://localhost:5173"

# Redis Cache (OPTIONAL - for production scaling)
# REDIS_URL="redis://localhost:6379"
